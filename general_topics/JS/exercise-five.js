/**
 * Exercise 5: Logical Operators
_________________________________________________

Objective: Use logical operators to evaluate multiple conditions.

1. Create three boolean variables, 'isAdult', 'hasJob', and 'hasDrivingLicense'.
2. Check if the person is an adult and has a job or a driving license and log the result.
 */

var isAdult = true;
var hasJob = false;
var hasDrivingLicense = true;

console.log(isAdult && (hasJob || hasDrivingLicense));
var x = 25 / 125 / 5;
console.log(x);

var x = 5;
var y = ++x; // x is incremented to 6, then y is assigned the value 6

var y = x++; // y is assigned the value 5, then x is incremented to 6

console.log(x);
console.log(y);
